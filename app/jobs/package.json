{
  "name": "cx-migration-jobs-ui",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:cf": "ui5 build --clean-dest --include-task=generateManifestBundle generateCachebusterInfo"
  },
  "devDependencies": {
    "@ui5/cli": "^3",
    "ui5-task-zipper": "^3",
    "ui5-tooling-modules": "^3"
  }
}
